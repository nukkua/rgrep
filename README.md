rgrep [OPTIONS] <PATTERN> [FILES...]

Options:
  -i        case insensitive
  -n        mostrar número de línea
  -c        solo contar matches
  -r        búsqueda recursiva en directorios
  -v        invertir match (líneas que NO matchean)
